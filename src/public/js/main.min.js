$(document).ready(function(){

  // if ( sessionStorage.getItem('popupPromotion') == null ) {
  //     setTimeout(() => {
  //         $('#popupPromotion').modal('show');
  //     }, 3000);
  //     sessionStorage.setItem('popupPromotion', 1);
  // }

  setTimeout(() => {
    $('#popupPromotion').modal('show');
  }, 3000);

  $(document).on('submit', 'form#contactForm', function(e) {
    e.preventDefault();

    window.location.href = "https://wocprint.com";

    // let formData = new FormData();

    // let fFullname = $('form#contactForm input#fullname').val();
    // let fPhone = $('form#contactForm input#phone').val();
    // let fEmail = $('form#contactForm input#email').val();
    // let fMessage = $('form#contactForm textarea#message').val();

    // if( fFullname != '' && fEmail != '' && fPhone != '' && fMessage != '' ) {
    //     formData.append('fFullname', fFullname);
    //     formData.append('fEmail', fEmail);
    //     formData.append('fPhone', fPhone);
    //     formData.append('fMessage', fMessage);
    //     formData.append('action', 'send_contact_email');
    // } else {
    //     Swal.fire({
    //         icon: 'warning',
    //         title: 'These fields can not be empty!',
    //         showConfirmButton: true,
    //     });
    // }

    // $.ajax( {
    //     url: myAjax.ajaxUrl,
    //     type: "POST",
    //     data: formData,
    //     enctype: 'multipart/form-data',
    //     processData: false,
    //     contentType: false,
    //     beforeSend: function() {
    //         $('form#contactForm button[type="submit"]').prop('disabled', true);
    //     }
    // }).done( function(rs) {
    //     Swal.fire({
    //         icon: 'success',
    //         title: 'Email has been sent successfully!',
    //         text: 'Thanks for contacting. We will respond to you as soon as possible!',
    //         showConfirmButton: true,
    //         timer: 3000,
    //         willClose: () => {
    //             location.reload();
    //         }
    //     }).then((result) => {
    //         if (result.isConfirmed) {
    //             location.reload();
    //         }
    //     });
    //     $('form#contactForm button[type="submit"]').prop('disabled', false);
    // });
  });
});
